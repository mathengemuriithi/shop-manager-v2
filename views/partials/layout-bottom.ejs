    </div><!-- end page content -->
    <div class="text-center mt-4 pt-3 border-t border-blue-900/30">
  <div class="flex items-center justify-center gap-3 flex-wrap">
    <div class="flex items-center gap-2">
      <span class="text-xs text-slate-500">📞</span>
      <span id="phoneNumber" class="text-xs text-slate-400 font-mono">+254 759 697 878</span>
      <button onclick="copyPhone()" class="text-xs bg-slate-800 hover:bg-slate-700 px-2 py-1 rounded transition-all text-slate-300">
        📋 Copy
      </button>
    </div>
    <div class="flex items-center gap-2">
      <span class="text-xs text-slate-500">✉️</span>
      <span class="text-xs text-slate-400">expertcyber.mwea@gmail.com</span>
    </div>
  </div>
  <p class="text-xs text-slate-600 mt-2">
    👁️ <%= totalViews || 0 %> total views · 👤 <%= uniqueVisitors || 0 %> unique visitors
  </p>
</div>

<script>
function copyPhone() {
  navigator.clipboard.writeText('+254759697878');
  const btn = event.target;
  const originalText = btn.innerText;
  btn.innerText = '✓ Copied!';
  setTimeout(() => { btn.innerText = originalText; }, 2000);
}
</script>
  </main><!-- end main -->
</body>
</html>
